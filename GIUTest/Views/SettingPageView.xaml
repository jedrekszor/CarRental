<UserControl x:Class="GIULayerV2.Views.SettingPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GIULayerV2"
             xmlns:p="clr-namespace:GIULayerV2.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <!--<UserControl.Resources>
        <local:SettingsPage x:Key="SettingsPage"/>
    </UserControl.Resources>-->

    <!--<Grid DataContext="{Binding Source={StaticResource SettingsPage}}">
        <TextBlock FontSize="20" Text="{Binding Path=PageTitle}" Margin="0,0,0,463"/>
        <Rectangle Margin="310,202,300,240" Fill="Fuchsia"/>
        <Button Command ="{Binding Path=GoToHome}" Content="Button" HorizontalAlignment="Left" Margin="452,402,0,0" VerticalAlignment="Top" Width="123" Height="50"/>
    </Grid>-->
</UserControl>
