<UserControl x:Class="GUILayerV4.Views.Pages.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUILayerV4.Views.Pages"
             xmlns:vmodel="clr-namespace:GUILayerV4.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <vmodel:MainWindowViewModel x:Key="MainPage"/>
    </UserControl.Resources>

    <Grid Background="GhostWhite">
        <Label Content="{Binding ClientInfo, Source={StaticResource MainPage}}" HorizontalAlignment="Left" Margin="650,10,0,0" VerticalAlignment="Top"/>

        <Button Content="Logout" HorizontalAlignment="Left" Margin="66,382,0,0" VerticalAlignment="Top" Width="75" Command="{Binding MainPage.Logout, Source={StaticResource MainPage}}"/>
    </Grid>
</UserControl>
