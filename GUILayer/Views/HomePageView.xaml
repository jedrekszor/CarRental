<UserControl x:Class="GUILayer.Views.HomePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vmb="clr-namespace:GUILayer.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <vmb:MainWindowViewModel x:Key="MainWindowViewModel"/>
    </UserControl.Resources>

    <Grid DataContext="{Binding Source={StaticResource MainWindowViewModel}}" Background="Aqua">
        <TextBlock FontSize="20" Text="{Binding HomePageViewModel.Model.PageTitle}" />
    </Grid>
    
</UserControl>
